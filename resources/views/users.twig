{% extends 'layouts/app.twig' %}

{% block title %} Profile for {{ user.username }} {% endblock %}

{% block content %}

  {#<ul>
      {% for user in users %}
          <li>{{ user.username }}</li>
          {% endfor %}
  </ul>#}
    <h1>{{ users.username }} (#{{ users.id }})</h1>

{% endblock %}